
import * as React from "react";
import Link from '../src/components/user/Link';
import * as renderer from 'react-test-renderer';

test('Link changes the class when hovered', () => {
  
  const component = renderer.create(
    <Link/>,
  );
 
 
});

